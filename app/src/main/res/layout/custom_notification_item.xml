<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/notification_ring"
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:layout_centerInParent="true"
        android:src="@drawable/baseline_notifications_none_24" />
    <TextView
        android:id="@+id/tv_notification_custom"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:background="@drawable/custom_notification_shap"
        android:layout_above="@+id/notification_ring"
        android:layout_toRightOf="@id/notification_ring"
        android:layout_marginStart="-15dp"
        android:layout_marginBottom="-15dp"
        android:text="9"
        android:gravity="center"
        android:inputType="number"
        android:textColor="@color/green"
        android:textSize="10dp"
        android:padding="4dp"
        tools:ignore="TextViewEdits" />
</RelativeLayout>